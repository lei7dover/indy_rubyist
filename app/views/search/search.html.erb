<h1><%=@message %></h1>

<div class= "row">
  <div class = "col-xs-offset-3">
  <% @rubyists.each do |r| %>
    <h3><%= link_to r[:name], r[:html_url]%> - <%=r[:followers]%> Followers - <%= r[:public_repos] %> Repos - Joined on <%= r[:created_at].to_s(:long) %>
  <% end %>
  </div>
</div>
